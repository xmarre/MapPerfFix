name: Preserve MapPerfProbe 2.1.2

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write

jobs:
  preserve-release:
    runs-on: windows-latest
    env:
      TARGET_SHA: 81f0d5a210885d1e5d5a1cc647242f5dbe9a91f3
      TAG: v2.1.2
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_SHA }}
          fetch-depth: 0
          persist-credentials: false

      - uses: microsoft/setup-msbuild@v2

      - name: Create compile-time TaleWorlds reference stubs
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $bin = Join-Path $env:GITHUB_WORKSPACE 'game\bin\Win64_Shipping_Client'
          $src = Join-Path $env:RUNNER_TEMP 'mapperf-stubs'
          New-Item -ItemType Directory -Force -Path $bin, $src | Out-Null

          @'
          namespace TaleWorlds.MountAndBlade
          {
              public abstract class MBSubModuleBase
              {
                  protected virtual void OnSubModuleLoad() { }
                  protected virtual void OnSubModuleUnloaded() { }
                  protected virtual void OnBeforeInitialModuleScreenSetAsRoot() { }
                  protected virtual void OnApplicationTick(float dt) { }
              }
          }
          '@ | Set-Content -Encoding UTF8 (Join-Path $src 'MountAndBlade.cs')

          @'
          namespace TaleWorlds.CampaignSystem
          {
              public enum CampaignTimeControlMode
              {
                  Stop = 0,
                  UnstoppablePlay = 1,
                  UnstoppableFastForward = 2,
                  StoppablePlay = 3,
                  StoppableFastForward = 4,
                  UnstoppableFastForwardForPartyWaitTime = 5,
                  FastForwardStop = 6
              }

              public class Campaign
              {
                  public static Campaign Current { get; }
                  public CampaignTimeControlMode TimeControlMode { get; }
              }
          }
          '@ | Set-Content -Encoding UTF8 (Join-Path $src 'CampaignSystem.cs')

          $vswhere = Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio\Installer\vswhere.exe'
          $vs = & $vswhere -latest -products * -requires Microsoft.Component.MSBuild -property installationPath
          if (-not $vs) { throw 'Visual Studio installation not found' }
          $csc = Join-Path $vs 'MSBuild\Current\Bin\Roslyn\csc.exe'
          if (-not (Test-Path $csc)) { throw "csc.exe not found at $csc" }

          & $csc /nologo /target:library /out:"$bin\TaleWorlds.MountAndBlade.dll" (Join-Path $src 'MountAndBlade.cs')
          if ($LASTEXITCODE -ne 0) { throw 'MountAndBlade reference stub compilation failed' }
          & $csc /nologo /target:library /out:"$bin\TaleWorlds.CampaignSystem.dll" (Join-Path $src 'CampaignSystem.cs')
          if ($LASTEXITCODE -ne 0) { throw 'CampaignSystem reference stub compilation failed' }

          foreach ($name in @('TaleWorlds.Core','TaleWorlds.InputSystem','TaleWorlds.Library','TaleWorlds.Engine','TaleWorlds.Localization','TaleWorlds.ObjectSystem')) {
              $empty = Join-Path $src (($name -replace '\.', '_') + '.cs')
              'internal static class ReferenceAssemblyMarker { }' | Set-Content -Encoding UTF8 $empty
              & $csc /nologo /target:library /out:"$bin\$name.dll" $empty
              if ($LASTEXITCODE -ne 0) { throw "$name reference stub compilation failed" }
          }

      - name: Restore and build preserved snapshot
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          msbuild MapPerfFix.sln /restore /m /p:Configuration=Release /p:Platform=x64 /p:BannerlordDir="$env:GITHUB_WORKSPACE\game"
          if ($LASTEXITCODE -ne 0) { throw 'MapPerfProbe build failed' }

      - name: Verify and package preserved snapshot
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          python tools/sync_version.py --check
          python tools/verify_safety.py

          $dll = Join-Path $env:GITHUB_WORKSPACE 'MapPerfFix\bin\x64\Release\MapPerfProbe.dll'
          if (-not (Test-Path $dll)) { throw "Expected output missing: $dll" }
          $version = [Reflection.AssemblyName]::GetAssemblyName($dll).Version.ToString()
          if ($version -ne '2.1.2.0') { throw "Unexpected assembly version: $version" }

          $stage = Join-Path $env:RUNNER_TEMP 'MapPerfProbe-v2.1.2-legacy'
          $module = Join-Path $stage 'MapPerfProbe'
          $moduleBin = Join-Path $module 'bin\Win64_Shipping_Client'
          New-Item -ItemType Directory -Force -Path $moduleBin | Out-Null
          Copy-Item 'MapPerfFix\SubModule.xml' $module
          Copy-Item $dll $moduleBin
          $pdb = Join-Path $env:GITHUB_WORKSPACE 'MapPerfFix\bin\x64\Release\MapPerfProbe.pdb'
          if (Test-Path $pdb) { Copy-Item $pdb $moduleBin }
          Copy-Item 'README.md' $module
          Compress-Archive -Path (Join-Path $stage 'MapPerfProbe') -DestinationPath (Join-Path $env:GITHUB_WORKSPACE 'MapPerfProbe-v2.1.2-legacy.zip') -Force

      - name: Create preserved tag and release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git fetch --force --tags origin

          if git rev-parse -q --verify "refs/tags/${TAG}" >/dev/null; then
            existing="$(git rev-list -n 1 "${TAG}")"
            test "${existing}" = "${TARGET_SHA}"
          else
            git tag -a "${TAG}" "${TARGET_SHA}" -m "MapPerfProbe 2.1.2 preserved before the Bannerlord 1.3.15 / ToR WiTM 1.16 update"
            git push "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" "refs/tags/${TAG}"
          fi

          if gh release view "${TAG}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
            gh release upload "${TAG}" MapPerfProbe-v2.1.2-legacy.zip --clobber --repo "${GITHUB_REPOSITORY}"
          else
            gh release create "${TAG}" MapPerfProbe-v2.1.2-legacy.zip \
              --repo "${GITHUB_REPOSITORY}" \
              --verify-tag \
              --title "MapPerfProbe 2.1.2 — preserved legacy release" \
              --notes $'Preserved snapshot of MapPerfProbe 2.1.2 before the Bannerlord 1.3.15 and The Old Realms: War in the Mountains 1.16 update.\n\nTarget commit: `81f0d5a210885d1e5d5a1cc647242f5dbe9a91f3`.\n\nThe attached ZIP contains the compiled legacy module. GitHub also provides source archives.'
          fi
